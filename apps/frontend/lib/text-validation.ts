export const annotationTextAllowedDescription = "all characters are allowed.";

export function validateAnnotationText(value: string | null | undefined, fieldLabel: string): string | null {
  void value;
  void fieldLabel;
  return null;
}
